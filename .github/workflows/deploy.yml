name: Legacy deploy (disabled)

# This legacy workflow used peaceiris/actions-gh-pages and was causing
# permission and deprecated-artifact errors when triggered on push.
# It's been disabled to avoid duplicate runs. Use `deploy-pages.yml` instead.

on:
  workflow_dispatch:
    # Intentionally empty to keep this legacy workflow disabled.
    # Use `deploy-pages.yml` (official) for GitHub Pages deployments.

# jobs intentionally omitted to keep this legacy workflow disabled.
jobs: {}
